version: 0.2
phases:
    install:
        commands:
            - 'npm install'
    pre_build:
        commands: []
    build:
        commands:
            - 'npm run build'
    post_build:
        commands:
            - aws s3 sync --acl public-read --delete build/ "s3://aemilialambdaedgedemo"
    serve:
        commands: []
