version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    # IMPORTANT - Please verify your build commands
    build:
      commands: []
  artifacts:
    baseDirectory: build
    discardPaths: true
    files:
      - '**/*'
  cache:e
    paths:
      - node_modules/**/*
